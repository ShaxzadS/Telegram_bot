create table atm (
    atm_number varchar(32) primary key,
    model varchar(255) not null,
    organization varchar(255) not null,
    address varchar(500) not null,
    sector varchar(255) not null
);
